import 'package:flutter/cupertino.dart';

class ImageOption extends StatelessWidget {
  const ImageOption({Key? key, required this.imageSource}) : super(key: key);

  final Function(bool) imageSource;

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
